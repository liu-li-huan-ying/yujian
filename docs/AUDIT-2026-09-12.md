# 项目审计报告 · 玉笺 Markdown 编辑器（yujian）

> 审计日期：2026-09-12
> 审计人：WorkBuddy（应主人指令"在认真检查项目管理代码质量和开发进度等方面"执行）
> 范围：`git` 仓库健康 / 发布节奏 / 路线图进度 / `src`+`electron` 代码质量 / 架构文档与代码现实 / 测试与 CI
> 方法：只读核查——`git` 历史与统计、全仓标记扫描、测试脚本断言计数、CI 工作流审查、已知问题文档与代码交叉验证。**未改动任何生产代码。**

---

## 零、总体结论

| 维度 | 评价 | 一句话 |
| --- | --- | --- |
| 仓库健康 / 单人工程纪律 | **好** | 单一作者邮箱、3 个发布标签、149 次提交、conventional-commit 极规范 |
| 数据安全红线 | **好** | 原子写 / 关联数据随迁 / 回收站 / 测试守护，四处一致 |
| 代码质量（静态） | **好** | 0 个 `TODO/FIXME/HACK`；`any` 44 处（较评审 61 处续降）；死代码清理彻底 |
| 测试与 CI 成熟度 | **好（对个人项目超配）** | test-core 181 + verify:md 26 + verify:corpus 19 + perf 基线，全进 CI |
| 架构文档与代码现实 | **基本对齐，个别文档陈旧** | eqref 三处矛盾已调和；CODE-REVIEW 数字已过时 |
| 进度 vs 路线图 | **约 75–80% 完成 Phase 3** | 批次 0–3 全落地，批次 4 进行中，批次 5 未动工 |
| 风险集中度 | **中低** | 真实风险是"文档陈旧"和"App.vue 回涨"，非功能缺陷 |

**核心判断**：工程质量底子明显优于一般个人项目，**当前最大短板不是代码缺陷，而是"质量基线文档没有跟上 09-11/09-12 的新功能"**，以及一处被新功能部分抵消的"上帝组件"回涨。没有发现会损及用户数据的活跃缺陷。

---

## 一、项目管理与开发进度

### 1.1 仓库健康（证据）

- `git shortlog`：全部 **149 次提交归于一枚作者邮箱** `<3169726897@qq.com>`，无第二作者污染贡献者图。
- 分支：`main` 单线 + `origin/main` 同步（`git status` 干净，无未推送提交）。
- 标签：`v1.0.0`(08-28) / `v1.1.0`(09-01) / `v2.0.0`(09-11)。版本号与功能里程碑对应清晰。
- 提交密度（按天）：08-28→09-04 为密集期（峰值 08-29 共 39 次），09-06→09-09 出现 **4 天空白**，09-10→09-12 恢复。节奏偏爆发式，但总量合理。

### 1.2 发布节奏

- 发布流程：打 `v*` 标签 → `release.yml` 三平台并行构建（win/mac/linux）→ 产物发布至 GitHub Release。
- CI 镜像源已显式切回官方（`npm_config_registry` + `ELECTRON_MIRROR`），规避了仓库内 `.npmrc` 的国内镜像在 CI 失效的风险——这是有意识的健壮性处理。
- ⚠️ **真实分发限制**：`release.yml` 中 `CSC_IDENTITY_AUTO_DISCOVERY: "false"` 且 macOS 无签名证书（主人明确要求"苹果版本不搞签名"）。后果：**未签名的 macOS 包在非开发者 Mac 上会被 Gatekeeper 拦截**，普通用户需手动绕过。即"跨平台"对 Windows/Linux 用户成立，对 Mac 普通用户实质不成立。属已知决策，但应在 README/发布说明里明确标注"macOS 需自行绕过 Gatekeeper 或仅开发者可用"，避免用户困惑。

### 1.3 进度 vs 路线图（PHASE3-PLAN.md）

| 批次 | 内容 | 状态 |
| --- | --- | --- |
| 零 | 缺陷修复 + 统一索引地基 | ✅ 已完成（除 eqref 端到端待 DOM 取证） |
| 一 | 数据安全（回收站/历史/原子写） | ✅ 已完成 |
| 二 | 双链 + 反链 | ✅ 已完成 |
| 三 | 标签 + MOC + 关系图谱 | ✅ 已完成（图谱 09-11 落地） |
| 四 | 中文排版 + 体验 | 🔶 **进行中**：命令面板 ✅（09-11）、空库引导 ✅、Maple Mono ✅、自然排序 ✅；"中文排版打磨 / 体验细节"尚未系统性收尾 |
| 五 | 技术写作 + 发布 | ⬜ 未动工（发布本身已完成 v2.0.0，但"技术写作"文档化批次四细节未做） |

**结论**：Phase 3 约 **75–80%** 完成。批次四剩余的是体验打磨（无数据风险），批次五主要是把已落地的设计写成"技术写作"文档。

### 1.4 项目管理风险

- **P2 · 进度追踪无量化机制**：仅靠 `PHASE3-PLAN.md` 与 `.workbuddy/memory/` 人工维护，仓库内无 issue/里程碑/看板。单人项目可接受，但缺少"本迭代完成了什么、还剩什么"的机器可读快照。
- **P2 · 历史提交残留 Claude 尾署**：`git log --all --grep="Co-Authored-By: Claude"` 命中 **3 个提交**（`0aa4185` / `e27e939` / `fc2d18f`，均 2026-08-31 eqref 排查期）。作者邮箱早已统一，GitHub 贡献者图不会显示 Claude，但**原始提交对象里仍嵌着尾署**。若要"历史彻底干净"需交互式 rebase 改写这 3 个旧提交（会改 hash、需 force-push，有破坏风险，不推荐除非必要）。当前无害。

---

## 二、代码质量与技术债

### 2.1 亮点（证据）

- **0 个 `TODO`/`FIXME`/`HACK`/`XXX` 标记**：全仓扫描（`src`+`electron`）只有"已知可容忍失败""临时文件"等**设计注释**，无遗留债务标记。
- **`any` 用量续降**：当前 44 处（评审基线 61→64），全部集中在 `src/editor/features/*` 与 Milkdown/micromark 框架回调边界，eslint 按路径精确豁免。
- **死代码清理彻底**：`RailView`（轨道视图）、`FILE_LIST_DIR`（死通道）、`minisearch`（死依赖）均经 `grep` 零命中确认已移除——之前评审列的 P1/P2 死代码项**全部闭环**。
- **容错可观测**：主进程 44 处容错 `catch` 统一经 `reportSoftError` 透出到完整性面板（评审 P1 已落地），且顺带修掉"vaultRoot 未就绪时静默跳过关联迁移"的隐患。
- **修复均带回归测试**：本次核查确认 `\eqref` 的"token 失效永久挂起"缺陷（`flushPendingRefs` 曾直接 `return`）**已在代码修复**（`src/editor/features/mathjax.ts:236-239` 改为 `task.resolve(svg)`），且 **verify-markdown.mjs:133-143 有专门回归断言**（"token 失效后引用任务仍会结算（不挂起）"）。评审核对的 REVIEW-OPTIONAL 高价值项（抽 diff 引擎、remap 进 store）也均已落地：`src/store/tabs.ts:91` 新增 `remap` action，`App.vue` 已改调 `tabs.remap(...)`。

### 2.2 风险

- **P1 · App.vue 回涨（"上帝组件"风险部分回归）**：
  - 评审（09-10）抽 composable 后报 `App.vue` **1683 行**；
  - 当前实测 **1789 行**（+106）。
  - 原因：09-11/09-12 落地的**命令面板、关系图谱、双栏停靠布局**又把协调逻辑堆回 `App.vue`，而 composable 抽取没跟上。这并不等于"之前白做"——`usePkmPanels`/`useVaultLinks` 仍在，但新功能又在同一处累积。
  - 建议：命令面板（`commands.ts` 已是单一事实源，耦合低）与图谱激活拦截逻辑可继续抽 composable；不必整体搬迁，按能力一次一块。
- **P2 · 仍偏大但属 CSS 撑大**：`Sidebar.vue` 1611 行（脚本 637 / 模板 386 / 样式 583）、`SnapshotPanel.vue` 1377 行（脚本 372 / 样式 861）。评审已正确诊断"大是 CSS 不是逻辑"——`SnapshotPanel` 的 diff 引擎已抽出（`snapshotDiff.ts`），剩 861 行样式属正常。`Sidebar` 的搜索/替换块（约 220 行脚本）是下一个合理抽取候选，但价值中等（评审定为"可做"），未做不扣分。

---

## 三、架构文档与代码现实核对

### 3.1 已对齐（之前评审指出的矛盾已调和）

- **eqref 三处矛盾已解决**：`ARCHITECTURE.md §5.3.2` 现表述为"已实现的修复，**不等于端到端已根治**"，并明确"用户实测的重载持久 `???` 尚未在本环境复现、也未经 DOM 取证定论"；`PHASE3-PLAN.md` 的 eqref 行已统一为"⚠️ 待定论"（此前一处写 ✅、一处写 ⚠️ 的自相矛盾已修）；`refAutoInputRule`（全库零命中的虚构符号）已从论证中剔除。
- **死代码引用已无**：文档提到的 `FILE_LIST_DIR` / `RailView` 在代码与文档中均不再出现。
- **mathjax 路径小误差**：评审文档写 `src/editor/mathjax.ts`，实际为 `src/editor/features/mathjax.ts`——仅路径写法差异，无实质误导。

### 3.2 落差（文档陈旧）

- **P1 · CODE-REVIEW-2026-09-10.md 已显著过时**，作为"质量基线文档"会误导后续读者：
  - 断言数：文档称 **124**，实测 **181**（test-core 段 A–L，含新增 I/J/K/L）；
  - 双语键：文档称 **511**，实测 **581**（i18n `[E]` 段对齐）；
  - 语料：文档称 **9** 个 `.md`，实测 `tests/corpus/` **19** 个；
  - **重大遗漏**：该文档写于命令面板/关系图谱/空库引导落地**之前**，完全未覆盖 09-11/09-12 的三大功能（命令面板、Canvas 关系图谱、空库《使用说明》播种），也未记录 REVIEW-OPTIONAL 的落地结果。
  - 建议：要么补一节"09-11/09-12 增量审计"，要么把过时数字校正——否则它描述的"当前状态"与代码偏差越来越大。
- **P2 · verify:md 数字漂移**：文档/记忆称 verify:md 29–30 条，实测 **26** 条 `t()` 断言。属小类，但同样应同步。

---

## 四、测试与 CI 成熟度

### 4.1 强项（证据）

| 套件 | 断言数 | 覆盖 |
| --- | --- | --- |
| `scripts/test-core.mjs`（`npm test`） | **181**（`check()` 调用） | A 引用改写 / B 索引纯函数 / C 端到端 fs / D wikilink 往返 / E 双语对齐 / F IPC 契约 / G 软错误 / H 数据安全线 / I 快照 diff / J frontmatter / K 标签重映射 / L 关系图谱派生 |
| `scripts/verify-markdown.mjs`（`npm run verify:md`） | **26**（`t()` 调用） | 数学渲染 / 内联 HTML / 主题令牌 / eqref token 失效回归 |
| `scripts/verify-corpus.mjs`（`npm run verify:corpus`） | **19**（`check()` 调用） | 19 个语料文件逐字节往返 |
| `scripts/perf-index.mjs`（`npm run perf:index`） | 基线断言（规模无关性） | 3000 文件索引性能 + 严格增量性 |

- **CI（`.github/workflows/ci.yml`）**：`push: main` + `pull_request` 触发，串行跑 `typecheck → lint → check:encoding → test → verify:md → verify:corpus → perf:index → build`，且 `concurrency.cancel-in-progress` 防排队。对个人项目属**超配**，是质量护栏的核心。
- **`check:encoding` 门禁**（防 CJK 经 Edit 往返静默损坏为 U+FFFD）是本项目特有且必要的防线，已进 CI。
- **零新测试框架**：全部用 esbuild 打 mjs 后在 Node 直跑，不启 Electron——与"优先复用现有依赖、不轻易加包"原则一致。

### 4.2 缺口 / 观察

- **组件层零自动化测试**：`src/` 下 35 个 Vue 组件 + 编辑器 feature 无单测。评审已明确"故意不引入 `@vue/test-utils`"，继续走"逻辑抽纯模块再测"路线——属有意识的取舍，非疏漏。
- **本地 `npm run check` 未含 `perf:index`**：本地门禁 = typecheck+lint+encoding+test+verify:md+verify:corpus；`perf:index` 只在 CI 跑。无碍（CI 覆盖即可），但本地与 CI 门禁不完全一致，偶发"本地过了 CI 挂"的 perf 类波动需靠 CI 兜底。
- **verify:md 实际 26 条**（文档称 29/30）：见 §3.2，纯文档同步问题。

---

## 五、问题清单（按严重度）

### P0 · 必修
- 无。本次核查未发现任何会损及用户数据或导致功能失效的活跃缺陷。

### P1 · 应修（影响可维护性 / 文档可信度）
1. **`App.vue` 回涨至 1789 行**（较评审 1683 增 106）——新功能（命令面板/图谱/停靠）协调逻辑再堆回，composable 抽取需跟进。
2. **`CODE-REVIEW-2026-09-10.md` 显著过时**——断言数(124→181)、双语键(511→581)、语料(9→19)全错，且漏覆盖 09-11/09-12 三大功能与 REVIEW-OPTIONAL 落地结果。作为"质量基线文档"会误导。

### P2 · 建议（低优先级 / 已知决策）
3. **macOS 未签名分发限制**——README/发布说明应明示"Mac 普通用户需绕过 Gatekeeper"，避免"跨平台"预期落差。
4. **3 个历史提交残留 `Co-Authored-By: Claude` 尾署**——无害（作者邮箱已统一），仅历史洁癖；改写需 rebase + force-push，不推荐除非必要。
5. **进度追踪无机器可读快照**——可加一份 `STATUS.md` 或 GitHub Projects，单人项目非必须。
6. **verify:md 数字文档漂移**（称 29/30，实测 26）——随 P1-2 一并校正。
7. **`Sidebar.vue` 搜索/替换块**（~220 行脚本）是下一个合理 composable 候选——价值中等，择机做。

### 已知开放项（非缺陷，已良好处置）
- **`\eqref` 端到端"重载持久 `???`"**：纯逻辑层三坑已修、token 失效挂起已修+回归测试；仅"Electron 重载时块级 `renderPreview` 是否派发"未做 DOM 取证。项目以"**不凭推测改代码**"的纪律妥善处理（两次凭推测修复均被推翻的教训已写入文档），列为开放项而非缺陷。**切勿在抓到 DOM 证据前改块级预览接线**（铁律：`crepe.editor.config(...)` 会中断 init 致图片全裂）。

---

## 六、优先级建议（可执行，按 ROI）

1. **【P1·低成本】校正/补强 CODE-REVIEW 文档**：在 `docs/CODE-REVIEW-2026-09-10.md` 末尾加一节"09-11/09-12 增量审计"，同步数字（181/581/19）并补命令面板、关系图谱、空库引导、REVIEW-OPTIONAL 落地结果。或重命名为带日期的滚动文档。
2. **【P1·中成本】继续抽 `App.vue` composable**：先把命令面板激活/图谱拦截抽成 `useCommandPalette()` / `useGraphView()`，目标把 `App.vue` 压回 ~1650 行以内；不为抽而抽，按能力一次一块。
3. **【P2·零成本】README/发布说明补 macOS 签名说明**：一句话标注未签名 Mac 包的使用前提。
4. **【P2·可选】`Sidebar` 搜索块抽 `useSidebarSearch()`**：价值中等，择机。
5. **【保留纪律】eqref 端到端取证前不动代码**：继续遵守"先抓 DOM 证据再改"的铁律。
6. **【不推荐】历史提交改写去 Claude 尾署**：收益极低、风险（改 hash/force-push）不成正比，除非主人有洁癖诉求。

---

## 附：核查证据索引

- 仓库：`git log` 149 提交 / 3 标签 / 单作者邮箱；`git log --all --grep="Co-Authored-By: Claude"` → 3 提交。
- 行数：`App.vue` 1789 / `Sidebar.vue` 1611 / `SnapshotPanel.vue` 1377 / `vault.ts` 1153 / `vaultIndex.ts` 1045。
- 标记扫描：`src`+`electron` 无 `TODO/FIXME/HACK`；`any` 44 处；`RailView`/`FILE_LIST_DIR`/`minisearch` 零命中。
- 测试断言：`test-core.mjs` 181 × `check(`；`verify-markdown.mjs` 26 × `t(`；`verify-corpus.mjs` 19 × `check(`；`tests/corpus/` 19 文件。
- CI：`.github/workflows/ci.yml`（main+PR 全门禁）、`release.yml`（三平台、macOS 未签名）。
- 代码核对：`src/editor/features/mathjax.ts:236-239`（token 失效 resolve）、`verify-markdown.mjs:133-143`（回归断言）、`src/store/tabs.ts:91`（`remap` action）。
