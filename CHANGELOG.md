# 更新日志 · 玉笺 Markdown 编辑器

本文件按版本记录重大变更。版本号遵循语义化版本（SemVer）；应用版本在 `package.json` 中维护，运行时动态读取。

---

## 未发布（开发中）

**主题：Phase 3 批次四 · 中文排版（渲染层）+ 大文档分块渲染 + 中文分词 + 快捷键自定义**

### 新增 · 中文双击选词（分词）
- **双击中文按词选中**：此前双击中文会把一长串汉字（常含标点）整段选走，想复制一个词、给一个词加粗都很别扭。现在双击中文按**词边界**选中，双击「开源」就选中「开源」。
- **不引词典、不改源文件**：分词用浏览器原生的系统级分词能力（与系统输入法同源），对系统未收录的双字词会自动回退成双字，避免只选中一个字；中文标点上双击选中的是该标点本身。
- **只接管中文**：英文、数字的双击仍走编辑器原有逻辑（它对西文处理得更好）；三击选段落也不变。

### 新增 · 大文档分块渲染（10 万字不卡）
- **屏外块跳过渲染**：对超过 2 万字符的文档，段落 / 标题 / 列表 / 引用 / 分隔线自动启用浏览器原生 `content-visibility: auto`，滚动时仅渲染可视区域附近的块，长文档滚动与输入保持顺滑。**不采用 DOM 窗口化**——真正的虚拟滚动会把屏外块从 DOM 中摘除，破坏 ProseMirror 的选区 / 输入法 / 装饰 / 查找前提；本方案让 DOM 保持完整，上述能力一律不受影响，也**绝不改动源文件**。
- **自适应，普通文档零影响**：仅当文档内容超过阈值时才启用，短文档完全不受影响。
- **滚动条不跳变**：浏览器会记住已渲染块的真实高度来估算总高，阅读位置与滚动条长度保持稳定。
- **顺带提速**：光标定位由「遍历全篇找当前段落」改为「自光标向上定位」，表情短代码的装饰结果按文档变化缓存，避免大文档下每次按键重复全量扫描。

### 修复 · 中文搜索「全词匹配」形同虚设
- **问题**：勾选「全词匹配」后搜「开」，仍会在「开源」「开会」里命中 —— 因为编辑器底层的词边界用的是 JS 原生 `\b`，而 `\b` **只认 ASCII 字母数字**，所有汉字在它眼里都是「非词字符」，于是「开」的左右两侧全被当成单词边界，全词匹配对中文完全是空操作。
- **修复**：改用 Unicode 词边界（`\p{L}\p{N}_` + 前后查）。现在中英文语义一致：搜「开」不再命中「开源」「开会」，但能命中被空格、标点或行首行尾包围的独立「开」。
- **库级搜索一并修好**：全库搜索原先另写了一套正则（同样的 `\b` 问题），现已与编辑器内搜索收敛到同一份实现，行为不再分叉。

### 改进 · 组件瘦身与结构门禁（无功能变化）

按 `docs/AUDIT-2026-09-14.md` 的审计结论做了一次结构性重构，**用户可见行为不变**，但把两块最容易腐化的代码从组件里搬了出来并纳入测试：

- **`App.vue` 1797 → 1592 行**、**`Sidebar.vue` 1611 → 1436 行**：导出（单文档 / 多文件合订 / 预览 / 写盘）与侧栏搜索替换各自抽为
  `composables/useExport.ts`、`composables/useSidebarSearch.ts`；两段内含的判定逻辑抽为零依赖纯函数
  （`export/exportMeta.ts`、`utils/searchNav.ts`、`utils/paletteHotkey.ts`），因此**首次可以被自动化断言**。
- **顺带修掉一处行为与注释不符**：命令面板已打开时，原实现是「再按任意热键都关闭」，而注释写的是「再按**同一个**键才关闭」。现按注释修正——`Ctrl+K` 与 `Ctrl+Shift+P` 各管一摊，互不误关。
- **新增结构门禁 `npm run check:structure`（已入 CI）**，专管不会让测试变红的慢劣化：巨石文件行数上限、
  `any` 逃逸越界与只减不增、`TODO/FIXME/HACK` 必须为零。
- 分层约定已入档：`docs/ARCHITECTURE.md` §5.33。测试断言 245 → 264。

### 新增 · 公式编辑器（复杂元素临时编辑界面）
- **点公式即可专注编辑**：点击 / 选中行内公式会弹出玻璃浮层 —— 上半输入 LaTeX 源码（等宽字体），下半**实时预览**，底部显示 `\label` 与编号状态；`Esc` 或点击外部即应用，右上角 `×` 取消。打开期间该公式会描一圈强调色边框。
- **预览与成品同源**：预览直接复用文档渲染用的 MathJax，所见即所得，不会再出现「编辑时看到的和最终排出来的不一致」。
- **常用符号工具条**：分希腊字母 / 运算 / 结构 / 标注四组，点一下即插入到光标处，光标还会自动落进需要填内容的括号里。
- **说明**：表格（增删行列、对齐）与代码块（语言、复制、行号、高亮）沿用编辑器既有的成熟能力，未重复实现；**表格列宽拖拽暂未提供** —— Markdown 表格语法本身不保存列宽，拖出来的宽度重载即丢，与其给一个会静默失效的控件，不如先不做。
- 可交互效果预览：[`docs/preview/math-edit-demo.html`](./docs/preview/math-edit-demo.html)。

### 新增 · 中文排版（纯渲染层，源文件零改写）
- **中英 / 中数 ¼ em 自动间距**：汉字与拉丁字母、数字之间由排版引擎自动补 ¼ em 间隙（W3C《中文排版需求》§3.2.2 建议的引擎行为），与全角标点相邻不补；代码块内不生效，行内代码仍补。
- **中文强调禁用斜体**：`em` 改用强调色（中文斜体公认难看，规范建议以颜色 / 着重号替代），英文斜体一并变色。
- **标点挤压 + 避头尾**：连续全角标点收紧、行首行尾标点不再突兀。
- **中文段落间距 +4px**：补偿全角标点的视觉密度。
- **偏好设置新增「中文排版」小节**：总开关 + 四项子开关，默认全开，改动即生效并持久化。
- 实现为标准 CSS（`text-autospace` / `text-spacing-trim`，Chromium 140+ 原生支持，Electron 44 已满足），无 JS 装饰、零性能开销；**保存时绝不改写源文件**。
- 可交互效果预览：[`docs/preview/cjk-typography.html`](./docs/preview/cjk-typography.html)。

### 新增 · 快捷键自定义（设置面板可改键位）

- **专用「快捷键」设置面板**：偏好 / 外观之外的独立面板，逐条列出全部命令、右侧显示当前键位胶囊；点胶囊即进入录入态，**捕获下一个按键组合**（捕获阶段接管 + `stopPropagation`，`Tab` 放行、`Esc` 取消、`Delete`/`Backspace` 显式解绑）。冲突时红字提示「被谁占用」，可一键替换或取消。支持过滤与「恢复默认」。
- **默认键位只有一份真相**：命令目录 `src/utils/commands.ts` 的 `keys` 字段是默认键位的**单一事实来源**，不再另存第二份表。命令面板键帽、设置面板、运行时派发表全部同源读取，**彻底消灭「两处键位表打架」**（原 HelpPanel 里手抄的那份键位表已删除，它一旦键位可配必然过期）。
- **安全底线 `isBindable`**：只按一个字母键**不能**当全局快捷键（否则正文打不出那个字）；要求带 `Ctrl`/`Alt`，或本身是 `F1–F12`。裸字母即便被手动塞进 `localStorage`，读盘时也会被丢弃、进不了派发表（单测锁死这一不变量）。
- **保留键位抢不走**：`RESERVED` 分 `app`（命令面板 / 快速打开入口，改掉会把自己锁在门外）、`system`（Electron / 浏览器先手的 `Ctrl+N/T/W/R/Q`、打印、`F5/F11/F12`、`Alt+F4`、开发者工具）、`editor`（正文在用的 `Ctrl+A/C/X/V/Z/Y/B/I/U`）。撞命令默认可抢占并清空原主人；撞保留键即使强制也返回失败。
- **只存覆盖 + `''` 表示显式解绑**：`localStorage` 只写被改过的命令，默认值改了用户没碰过的命令会跟着升级走，不会「旧键位被钉死」；存储后端可注入便于单测。
- **派发改为查表**：`App.vue` 的 `onKeydown` 删掉全部 `if (k==='…')` 硬编码分支，改为 `buildDispatchTable()` 反向索引（`规范串 → 命令 id`）后 `commandActions[id]()`，命令增多不再失控、且天然可配。
- **搜索三件套提成命令、一并可配**：`view.search`(Ctrl+F) / `view.nextHit`(F3) / `view.prevHit`(Shift+F3) 原硬编码在 `App.vue`，现纳入命令目录，与全局快捷键同等待遇（同样受保留键约束）。
- **新增命令动作**：`settings.shortcuts`（**F1** 打开快捷键设置）、`view.search` / `view.nextHit` / `view.prevHit`。原 F1 的「帮助」入口迁到标题栏「？」按钮，帮助面板精简为**使用指南 + 关于**两页（不再含过期的手抄键位表）。
- 可交互效果与色板自查：见 `docs/preview/`（同批次界面体验修复对照）；配套回归测试 `scripts/test-core.mjs` `[R]` 段 **21 + 3** 条（含裸字母拦截、派发表只含可绑定键位、存储非可绑定覆盖被丢弃）。

### 修复 · 界面体验
- **命令面板不再「多行同时高亮」**：此前打开命令面板时，每个分组的第一条命令都会一起显示为选中态。根因是高亮判断误用了「分组内序号」而非「跨分组全局序号」，`activeIndex = 0` 时每组第一行同时命中（同一原因还导致行 id 在不同分组间重复）。现统一改用全局序号，**只有真正选中的那一行高亮**，键盘上下键与鼠标悬停的定位也随之准确。
- **阅读进度条不再「莫名跳动」**：此前开关侧栏 / 大纲、点击标题栏按钮时，进度条会自己走一格。根因是进度按「滚动位置 ÷ 可滚动高度」计算，而面板开合会让正文重新排版、改变可滚动高度，即使用户没滚动也会被当成进度变化。现在只有**真正的滚动**才会改变进度，纯粹的重新排版不再影响刻度；同时加了微小变化的死区（首尾 0% / 100% 仍精确到头）与内容变化防抖，既稳又不拖慢输入。
- 可交互修复对照：[`docs/preview/reading-progress-fix.html`](./docs/preview/reading-progress-fix.html)（左右并排「修复前 / 修复后」，点按钮即可看到刻度是否被重排改写）。

---

## v2.1.0 — 2026-09-12

**主题：修复数学公式编号引用 `\eqref` 重载后持久显示 `???`**

这是一次聚焦修复的维护性发布，不含功能新增。

### 修复 · 数学公式编号引用
- **根治行内 `\eqref` 重载持久 `???`**：根因并非此前怀疑的「KaTeX 覆盖竞态」或「重载时序竞态」，而是 **late-label gap**——CodeMirror 块级预览通过 `IntersectionObserver` 懒初始化，块级 `\label` 的注册**晚于**行内 `\eqref` 的引用结算；一旦行内引用在超时后已回落为 `(???)` 并「定型」，后续迟到的 `\label` 再无机制唤醒它重渲染，已解析的 NodeView 永远不会刷新。
- **修复机制**：在公式编号标签状态中新增 `labelChangeListeners` 通知（`onLabelsChanged`）。当某个 `\label` 注册或更新时，向所有**仍处于未解析状态**（`refDisplay === '???'`）的行内 `\eqref` 派发重渲染；若此时标签已可用，则立即结算为正确编号；与 `pendingRefs`（仅唤醒尚未定型的引用）互补，覆盖「先引用后定义 + 定义迟到」这一真实路径。已销毁的 NodeView 不会泄漏（监听器随节点销毁移除）。
- **配套回归测试**：`scripts/verify-markdown.mjs` 新增 1b（迟到标签恢复编号）、1c（无关标签不误触发）、1d（NodeView 已销毁不泄漏）、1e（快速编辑不报错）四组用例，连同既有的 token 失效 / 引用先于定义 / 超时兜底，`\eqref` 逻辑层覆盖已较完整。

### 文档
- `docs/ARCHITECTURE.md` §5.3.2 将 `\eqref` 验证状态由「部分修复·待定论」更正为「端到端已根治」，并补记 late-label 根因与 `labelChangeListeners` 机制。
- `.workbuddy/memory/EQREF-KNOWN-ISSUE.md` 归档为已解决，指明真实根因为 late-label gap 而非块级预览派发缺失。

---

## v2.0.0 — 2026-09-11

**主题：个人知识管理（PKM）体系成型 + 双栏停靠布局重构**

这是一次跨越 Phase 3 全部批次的大型发布。在 v1.1.0（Phase 2 全批次落地）基础上，补齐了 PKM 四件套（双链 / 标签 / 内容地图 / 关系图谱），并将原先的「轨道视图」收拢式布局彻底重构为「左缘活动栏 + 双栏 2×2 停靠」的打散式布局。

### 新增 · 个人知识管理（PKM）
- **统一 Vault 索引层**：替换 Minisearch 死依赖，建立轻量、严格增量的本地索引（仅存元数据，不缓存正文），为后续所有 PKM 功能提供单一事实来源。
- **数据安全与完整性**（批次一）：整库完整性自检与重建、整库备份、文件冲突「三选一」解决、表格压测。
- **双向链接**（批次二）：`[[wikilink]]` 升级为编辑内核真节点；新增反链面板、`[[` 自动补全、未链接提及自动包裹、断链一键创建。
- **标签系统**（批次三之一）：`#标签` 内联真节点 + frontmatter 双轨采集，新增标签聚合面板。
- **内容地图 MOC**（批次三之二）：frontmatter `moc` 标记 + 三组聚合面板（标签 / 内容地图 / 反链）。
- **关系图谱**（批次三之三）：由索引派生的全屏 Canvas 视图，支持本地子图 BFS、按度截断、平移/缩放/拖拽、无障碍列表视图与 `prefers-reduced-motion` 收敛终态。
- **引用自动改写**：重命名 / 移动文档（含整文件夹）时自动更新 `[[引用]]`，并随迁移历史与附件；以索引反查零全库扫描。

### 新增 · 交互与体验
- **命令面板**（批次四首项）：`Ctrl/Cmd+Shift+P` 统一命令入口，覆盖文件、视图、编辑、设置等操作。
- **双栏 2×2 停靠布局**：左列〔目录 / 标签·内容地图〕、右列〔大纲 / 反链·快照〕四位一体，PKM 面板不再全局互斥。
- **左缘活动栏**：将收拢在「更多」里的功能拆成常驻按钮，分散到界面四周，并废弃旧的轨道视图。
- **面板就近弹出**：库级面板落在左上、文档级面板落在右上，按活动栏分组。
- **玉质药丸芯片 `TagChip`**：消除面板内裸文字标签。
- **空库引导**：首次打开空库自动放入《使用说明》文档。
- **自然排序**：侧栏文件树按以人为本的顺序排列，支持「第一章 < 第五章 < 第七章」式中文数字词。
- **Maple Mono 字体**：默认字体升级为核心 6 款字重，标题 / 代码块 / 源码模式 / Mermaid 图文字统一覆盖。
- 选区高亮提实、列表标记自定义高对比、常态轻聚焦、链接悬停微交互等细节打磨。

### 工程与质量
- **软错误可观测**：主进程容错统一经 `reportSoftError`，前端「被容忍的失败」面板可见。
- **测试基建**：Markdown 往返语料矩阵（`verify:corpus`）、核心单测（`test-core`，含图谱派生断言）、性能门禁改为「规模无关性」断言、文本编码门禁（`check:encoding` 拦截 CJK 静默损坏）。
- **CI 升级**：工作流升级 Node 22 + `checkout`/`setup-node` v5；esbuild 改走 JS API 实现跨平台可复现打包。
- **依赖收敛**：App 瘦身、移除未用依赖、语法收敛。

### 已知限制
- macOS 本次发行为**未签名**构建（`identity: null` / `notarize: false`）：首次打开需右键「打开」并在系统设置中授权。若需签名 + 公证，在仓库 Secrets 配置 Apple 证书相关变量即可由 CI 自动接管。

---

## v1.1.0 — 2026-09-01

Phase 2 全部批次落地：多文档标签、查找替换、版本快照（git 化，段落级 cherry-pick）、写作统计、凝神 / 打字机 / 禅模式、导出增强（docx 脚注 / ePub / LaTeX / PDF）、写作辅助包、断链健康检查。三平台（Windows / macOS / Linux）CI 自动发布打通。

---

## v1.0.0 — 2026-08-28

首个正式版本：所见即所得（可切源码）的跨平台 Markdown 编辑器，基于 Electron 44 + Vue 3 + Milkdown Crepe，强调 Markdown 往返保真与本地优先。
